# Claude Adapter

Claude-specific convenience wrappers may exist under `.claude/skills/`, but canonical instructions still live under `.agents/skills/`.

When a Claude-facing skill wrapper exists, use it only as a pointer back to `.agents/skills/<skill>/SKILL.md`.
